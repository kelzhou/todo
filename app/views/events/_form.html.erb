
<div class="formtastic-form">
<%= semantic_form_for @event do |f| %>
  <%= f.semantic_errors %>  
  <%= f.input :user_id, :as => :select, :collection => User.all %>
  <%= f.input :title %>
  <%= f.input :date %>

  <%= f.action :submit %>

<% end %>
</div>


